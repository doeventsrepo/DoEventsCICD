# Copiar a simulation/local.env (gitignored) para pruebas locales
# CURSOR_API_KEY=           # Solo si modes.liveCursorAgent=true
# SIM_RUN_LIVE_AGENT=1      # Activa agente real (empuja a GitHub, no sandbox)
# AWS_ACCESS_KEY_ID_DEV=    # Solo con --deploy
# AWS_SECRET_ACCESS_KEY_DEV=
# DSF_LOCAL_USE_PRODUCTIVE_WEB=1  # Usar DoEventsWEB real en vez de sandbox (cuidado)
