# templates/ReglasAgente (deprecado)

Las plantillas bootstrap están en **`Reglas/artefactos-web/`**.

El script `scripts/bootstrap-web-reglas-agente.sh` y el workflow `lovable-sync-to-web.yml` copian desde allí hacia `DoEventsWEB/ReglasAgente/`.

No editar archivos en esta carpeta; usar `Reglas/artefactos-web/`.
